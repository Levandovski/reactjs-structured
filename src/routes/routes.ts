import { Login } from "../pages/login";
import { routesProps } from "./interface";

export const routes: routesProps[] = [
    {
        id: 0,
        path: '/login',
        component: Login,
    }
]  